
import os
import sys

pdbbindpath="/"
analypath="/"
datapath="/"

for file in os.listdir(pdbbindpath):
    current = os.path.join(path, file)
    # for each file with the suffix, "_protein.pdb"
    if os.path.isfile(current) and current.endswith("_protein.pdb":
        # import the file into a structure object
    	# run PH
		# output diagram to analypath
		# output data (?) to datapath


